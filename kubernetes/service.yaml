apiVersion: v1
kind: Service
metadata:
  name: zammad-service
  namespace: techassist
  labels:
    app: zammad
spec:
  type: NodePort
  selector:
    app: zammad
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
